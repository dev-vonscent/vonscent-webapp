auto-install-peers=true
verify-deps-before-run=false

# Vercel refuses a serverless bundle that contains symlinked directories, which
# is exactly what pnpm's default .pnpm/ store layout produces for sharp's native
# packages. A flat (npm-style) node_modules keeps real files where the tracer
# and the bundler both expect them.
node-linker=hoisted
