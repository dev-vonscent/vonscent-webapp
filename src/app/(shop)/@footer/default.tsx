/**
 * Every shop route other than `/`. A parallel slot needs a `default` for the
 * segments it does not match, and here that is simply no footer.
 */
export default function NoFooter() {
  return null;
}
