import { ProductFormSkeleton } from "@/components/shared/skeletons";

/** Same shape as the create form — see `products/new/loading.tsx`. */
export default function Loading() {
  return <ProductFormSkeleton />;
}
