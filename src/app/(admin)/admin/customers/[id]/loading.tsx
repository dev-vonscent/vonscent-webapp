import { DetailSkeleton } from "@/components/shared/skeletons";

/** One customer: their orders beside the profile panel. */
export default function Loading() {
  return <DetailSkeleton label="Хэрэглэгч ачаалж байна" />;
}
