import { DetailSkeleton } from "@/components/shared/skeletons";

/** One order: its line items beside the customer and payment panels. */
export default function Loading() {
  return <DetailSkeleton label="Захиалга ачаалж байна" />;
}
