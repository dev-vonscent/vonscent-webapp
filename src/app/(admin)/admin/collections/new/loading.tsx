import { ProductFormSkeleton } from "@/components/shared/skeletons";

/** Same shape as the product form's: a form is about to appear, not a table. */
export default function Loading() {
  return <ProductFormSkeleton />;
}
